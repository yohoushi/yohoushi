p style="font-size:x-large"
  - if @root.present?
    - @root.ancestors.each do |node|
      = link_to_node(node)
      font color="#bbb"
        |  » 
    = @root.basename
form class="form-search autocomplete-graph"
  input type="text" class="span11" id="autocomplete" placeholder="Type Graph Path" data-autocomplete=autocomplete_graph_path name="path" value=params[:path]
  button type="submit" class="btn"
    | Go
